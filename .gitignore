# Upstream reference clone (kept locally for porting, not part of this repo)
reference/

# .NET build artifacts
bin/
obj/
*.user
.vs/

# Rider / VS Code
.idea/
*.DotSettings.user

# OS
.DS_Store

# App runtime data / cores / ROMs (never commit)
Cores/
*.so
*.dll

# Spike scratch (downloaded cores/ROMs, throwaway)
spike/

# Local API keys — never commit
src/Emutastic/Secrets.cs

# Local pre-change backups (not versioned)
.snapshots/

# Internal handoff/findings notes — kept locally, not published
FINDINGS-SO-FAR.md

# Internal working docs — local only, never published
/notes/
